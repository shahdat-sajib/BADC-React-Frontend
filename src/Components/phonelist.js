import React from "react";
import { CheckCircle } from "@mui/icons-material";
import { Delete } from "@mui/icons-material";
export default function Phonelist({
    phone,
    toggleComplete,
    handleDelete,
    handleEdit
}) {
    // const [newTitle, setNewTitle] = React.useState(phone.title);

    return (
        <div>
            <h3>Im list</h3>
        </div>
    )
}